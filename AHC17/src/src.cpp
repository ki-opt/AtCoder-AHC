#include <iostream>
#include <vector>

using std::vector; using std::cout; using std::cin;

class instance {


};

int main() {
   vector<int> vec;
   cout << vec[0];
   cout << 1;
   cout << 2;
   return 0;
}